<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ProgressBar
        android:id="@+id/loading_item_progress_bar"
        android:layout_width="@dimen/adapter_loading_pb_size"
        android:layout_height="@dimen/adapter_loading_pb_size"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/adapter_loading_pb_top_margin" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/loading_item_progress_bar"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/adapter_loading_title_top_margin_height"
        android:text="@string/loading_item_title"
        android:textSize="@dimen/normal" />
</RelativeLayout>